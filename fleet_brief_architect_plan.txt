# ARCHITECT BRIEF — CASMI26 full-roadmap plan (make the plan, do not build)

You are the ARCHITECT. Write the plan for the ENTIRE remaining CASMI26 roadmap,
on-disk, in staged chunks. You are NOT executing any phase — you only plan.

## Read these (facts, do not re-derive; read each ONCE)
- ~/code/casmi26/docs/HANDOFF.md          — current state + the F-SPEED correction (critical)
- ~/code/casmi26/PLAN.md                   — the living master plan
- ~/code/casmi26/docs/FRANKER_PLAN.md      — existing F-RANKER scoping
- ~/code/casmi26/src/f_ranker.py           — the mostly-written F-RANKER code
- ~/code/casmi26/src/analog_run_v2.py      — the measured F-ANALOG wall (MRR 0.2125 / hit 62.75%)
- ~/code/casmi26/docs/FSPEED_REPORT.md     — new measured F-SPEED result (4.3ms, bit-exact, PASS)

## Goal
Top-10% CASMI26 medal (MRR~0.30+, winner ~0.339), deadline 2026-12-14. The game is
RANKING ~132 mass-matched candidates per query, not novel discovery. Current floor:
F-ANALOG analog-propagation MRR@25=0.2125 / hit@25=62.75%. F-SPEED (sim kernel) is
DONE and bit-exact-fast — it is NOT a bottleneck, don't plan around it.

## Your deliverable: ~/code/casmi26/docs/ARCHITECT_PLAN.md
Write it in STAGED CHUNKS (append section 1..N to the file, keep every write small;
return a short pointer as your final message). Required sections:
1. **Phases** — decompose the whole remaining roadmap into concrete gated phases
   (F-RANKER, then F-CONS / F-MASS / F-ALEIGN / F-EAERN-spec2vec, then ensemble +
   submission), each with: goal, exact gate (must BEAT incumbent — start 0.2125 MRR),
   dependency, and which Savant workspace/KG node it touches.
2. **Agent assignment — YOU decide the agents, for the ENTIRE deal.** The number of
   agents AND which role per phase is your call (full 13-persona fleet available:
   architect, engineer, programmer, researcher, solver, tester, checker, designer,
   writer, business, advisor, academic, trader — real profiles, all lane-routed to the
   AGY bridge). Per user doctrine: EVERY phase is executed by **TWO DIFFERENT agents**
   (a pair, so a single-agent wedge can't stall a phase) — distinct roles per phase,
   state who and why. State the TOTAL agent count for the whole deal.
3. **Slicing rule** — how each pair splits its phase (disjoint work, one per slice) so
   the two agents never collide on files.
4. **Lane + runtime** — every agent on the AGY bridge (:8790, gemini-3.8-flash-medium),
   never openrouter for execution. Bounded: max-turns + run-budget per agent.
5. **Handoff contract** — what each phase's agents must write to disk + how the next
   phase consumes it (lean pointers, not payloads).
6. **Critical-success factors / risks** — the known wedge risks (single large runs wedge;
   already proven: fleet CANNOT execute heavy CASMI compute in-session, AGY lane is the
   answer) and how the 2-agent pairs + write-to-disk-first mitigate them.

## Constraints
- Work ONLY in ~/code/casmi26. Do not touch data/ or re-download anything.
- Write to docs/ARCHITECT_PLAN.md incrementally. Real plan only — grounded in the files
  above, no invented metrics.
- If you hit your run/turn budget without a completed plan file, STOP and return:
  'NOT DONE — <exact list of what's missing>', exit non-zero. NEVER report a clean finish
  when docs/ARCHITECT_PLAN.md is incomplete.

Return: the absolute path to ARCHITECT_PLAN.md + a 10-line summary (phase list, per-phase
agents, total agent count, the gate for phase one).